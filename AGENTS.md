# Things Terminal 项目指南与更新计划

## 适用范围

本文件适用于仓库根目录及所有子目录。后续 Agent 在修改代码前应先阅读本文件，并以当前代码、数据库结构和用户最新要求为最终事实来源。

## 项目定位

Things Terminal 是一个个人设备与物品资产台账。它使用 Windows 98 风格的多页界面，记录设备的分类、品牌、状态、图片、购入/失去时间与地点、价格、币种、父级关系及规格，并提供展示、搜索筛选、录入、统计、详情和后台管理功能。

当前技术栈：

- Node.js 20、Express 4
- SQLite（`sqlite` + `sqlite3`）
- 原生 HTML、CSS、JavaScript ES Modules
- Multer 图片上传
- Docker / Docker Compose

## 当前架构

### 服务端与数据

- `server.js`：应用启动、数据库初始化/兼容迁移、种子导入、中间件和全部 REST API。目前职责较重。
- `data.js`：旧数据结构和首次启动种子。仅当 `devices` 表为空时用于初始化，不能把它误认为运行时数据源。
- `data/devices.sqlite`：当前仓库内的 SQLite 数据文件。现有快照包含真实演示数据；普通开发和自动化测试不得直接改写它。
- `public/uploads/`：运行时上传图片目录，由服务启动时创建；生产部署需要持久化。

核心表：

- `devices`：`id`、`name`、`category`、`status`、`imagePath`、购入/失去信息、`parent`、JSON 字符串形式的 `specs`、`brand`、买卖价格与币种。
- `categories`：唯一分类名称。

状态约定目前只有：

- `active`：使用中
- `deleted`：已失去；它是业务状态，不等于数据库记录已删除

### 前端

- `main.js`：统一加载元信息、分类和设备数据，再按当前页面初始化功能。
- `modules/api.js`：API 请求与管理密码请求头。
- `modules/store.js`：浏览器内共享状态；管理密码目前存放在 `localStorage`。
- `modules/schema.js`：少量前端数据规范化逻辑。
- `modules/render.js`：展示、筛选、统计、详情和新增页面逻辑。
- `modules/admin.js`：后台编辑、批量操作、图片上传及 JSON 导入导出。
- `index.html`、`browse.html`、`add.html`、`stats.html`、`device.html`、`admin.html`：多页入口。
- `styles.css`：全站样式；页面还从 unpkg 加载 `98.css`。
- `app.js`：模块化之前的旧版单文件前端，当前 HTML 没有引用。确认无遗漏功能后应删除，更新功能时不要同时维护两套实现。

## API 概览

公开读取：

- `GET /api/meta`
- `GET /api/categories`
- `GET /api/devices`
- `GET /api/devices/:id`

写入或导出接口经 `requireAdmin` 保护：

- `POST /api/uploads`
- `POST /api/categories`
- `POST /api/devices`
- `PUT /api/devices/:id`
- `DELETE /api/devices/:id`
- `POST /api/devices/bulk-update`
- `POST /api/devices/bulk-delete`
- `GET /api/export`
- `POST /api/import`

注意：当环境变量 `ADMIN_PASSWORD` 为空时，`requireAdmin` 会放行所有请求。因此任何非本机部署都必须显式设置管理密码；后续更新不应悄悄改变公开读取行为。

## 本地运行

首次运行：

```bash
npm install
npm run dev
```

生产方式：

```bash
npm start
```

默认地址为 `http://localhost:3000`，也可通过 `PORT` 修改。

Docker：

```bash
docker compose up --build
docker compose -f docker-compose.dev.yml up --build
```

仓库目前没有 `package-lock.json`，因此暂时不能把 `npm ci` 当作有效命令；第一阶段应补齐并提交锁文件。

## 修改规则

1. 保持项目简单、可本机自托管；在有明确收益前，不引入前端框架或外部数据库。
2. 数据库字段、API 请求/响应和表单字段必须同步更新。新增字段时还要覆盖导入、导出、种子、统计和详情视图。
3. 数据库迁移必须可重复执行，并兼容已有 `data/devices.sqlite`，不得要求用户手工清库。
4. 涉及删除、覆盖导入或结构迁移前先备份数据库。测试必须使用临时数据库，不得污染被 Git 跟踪的数据文件。
5. 所有来自数据库、表单、导入文件或 URL 的文本在写入 `innerHTML` 前必须转义；不要继续扩散字符串模板直接插入未清洗数据的模式。
6. 金额在存储和计算中使用数值与明确币种；不要从展示文本反推可用数据。汇率必须标注来源与时间，不能把硬编码估算值伪装成实时值。
7. 图片上传需同时验证大小、真实文件类型和安全扩展名；生成文件名，不使用客户端文件名作为路径。
8. 保留中文界面与 Windows 98 视觉语言，功能更新需兼顾窄屏、键盘操作和清晰的加载/错误反馈。
9. 不提交密码、令牌或本机专有配置。新增环境变量时同步提供 `.env.example`，只放安全示例。
10. 避免把新逻辑继续堆入 `server.js` 或 `modules/render.js`；新增较大功能时优先拆为职责清晰的模块。

## 当前已知风险与技术债

- 没有自动化测试、测试脚本和 CI，当前只能做语法检查与手工冒烟测试。
- 没有依赖锁文件，构建不可完全复现。
- 多处使用 `innerHTML` 拼接设备和导入数据，存在持久型 XSS 风险。
- 服务端输入校验较弱，状态、日期、币种、价格、ID 和导入内容缺少统一约束。
- 覆盖导入与批量操作没有事务保护，中途失败可能留下部分写入或空数据集。
- `server.js` 同时承担迁移、业务、路由和启动；价格解析逻辑在前后端重复。
- `app.js` 是未使用的旧实现，容易误导维护者。
- 统计页汇率硬编码在 `modules/render.js`，结果只能视为估算。
- 管理密码保存在浏览器 `localStorage`，服务端仅比较单个共享密码；不适合直接暴露到公网。
- SQLite 运行时数据库被 Git 跟踪，日常运行容易产生二进制 diff，也会混合种子与用户数据职责。
- 所有页面依赖 unpkg 上的 `98.css`，离线或 CDN 故障时样式会退化。
- 详情页模板中存在重复的 `.badge-row` 开始标签，需要在近期修复并做页面回归。

## 后续更新路线

按顺序推进。除非用户明确指定别的目标，不要跳过仍未满足的前置质量门。

### M0：建立可复现基线（最高优先级）

- [x] 添加 `README.md`，写明产品截图/功能、环境变量、运行、备份与部署流程。
- [x] 生成并提交 `package-lock.json`，统一使用 Node.js 20。
- [x] 添加 `.gitignore`，覆盖依赖、上传、临时数据库、日志和本机环境文件；现有数据库快照保留为只读种子 fixture，运行数据写入 `data/runtime/`。
- [x] 添加 `.env.example`，至少说明 `PORT` 与 `ADMIN_PASSWORD`。
- [x] 添加 `GET /health`，验证服务和数据库均可用。
- [x] 修复详情页重复标签，并确认六个页面均可加载。

完成标准：全新检出后可按文档启动；健康检查返回 200；页面和现有 13 个 API 路径完成一次冒烟验证；工作树不会因普通启动产生无关文件。

### M1：安全与数据正确性

- [x] 建立统一的 HTML 转义/安全 DOM 渲染方法，覆盖名称、分类、品牌、备注、规格、导入预览和管理列表。
- [x] 在服务端集中校验/规范化设备 payload：名称长度、允许状态、ISO 月份/日期、非负金额、受支持币种、整数 ID、规格数组上限。
- [x] 为 JSON 解析、数据库错误和异步路由增加一致的错误响应，避免异常导致进程退出。
- [x] 使用事务处理覆盖导入、覆盖导入、批量更新和批量删除；失败时完整回滚。
- [x] 收紧上传验证，阻止伪装文件和危险扩展名，并清理设备替换/删除后不再引用的图片。
- [x] 生产模式下若未设置 `ADMIN_PASSWORD`，拒绝启动；密码通过定长哈希和 `timingSafeEqual` 比较。

完成标准：恶意 HTML 作为普通文本显示；非法 payload 返回 4xx；事务故障不会产生半完成状态；上传与管理接口有自动化安全测试。

### M2：数据生命周期与迁移

- [x] 引入版本化迁移机制，不再用分散的 `PRAGMA` + `ALTER TABLE` 推断版本。
- [x] 将演示种子、测试 fixture 与用户数据库分离，并删除不再使用的 `data.js`。
- [x] 提供带时间戳的备份与恢复命令，并验证导出的 JSON 能在空库中恢复。
- [x] 停止跟踪运行时 SQLite 文件；仓库示例库复制为启动数据而不是原地运行。
- [x] 将文本 `parent` 升级为稳定的父设备 ID，同时提供兼容迁移和防循环校验。
- [x] 补齐分类重命名、删除/合并规则，避免孤立分类。

完成标准：旧数据库可无损升级；备份恢复校验前后设备数量和关键字段一致；测试和日常运行不修改版本库内的基准数据。

### M3：自动化测试与持续集成

- [x] 使用 Node 内置测试运行器，新增 `npm test`。
- [x] API 集成测试使用临时 SQLite 与临时上传目录，覆盖 CRUD、鉴权、批量操作、导入导出和错误路径。
- [x] 为价格、日期、持有天数、规格规范化与筛选关键规则增加单元/浏览器测试。
- [x] 增加浏览器冒烟测试，覆盖首页、搜索、新增、详情、统计和管理流程。
- [x] 添加 GitHub Actions，在 Node.js 20 上执行安装、语法检查、测试、安全审计和镜像构建。

完成标准：全新环境中一条测试命令可重复通过；CI 不依赖仓库中的用户数据库，也不需要外网 CDN 才能完成核心验证。

### M4：模块化与可维护性

- [x] 将 `server.js` 拆为配置、数据库、迁移、校验、服务和路由模块，并支持测试时创建但不监听的 app。
- [x] 抽出前后端共享的价格、日期和币种规则，消除重复解析。
- [x] 将 `modules/render.js` 按页面拆分到 `modules/views/`；用小型 DOM 工具减少大段不安全 HTML 字符串。
- [x] 核对旧 `app.js` 与模块化实现的功能差异后删除旧文件。
- [x] 为关键对象补充 JSDoc 类型，并设置轻量 `npm run check` 命令。

完成标准：路由和页面模块可独立测试；没有两套前端实现；新字段只需在明确的少数入口维护。

### M5：体验与资产管理能力

- [x] 保存筛选、排序和子设备显示偏好，并允许从详情页返回原筛选上下文。
- [x] 改进空状态、加载状态、保存反馈、删除确认和移动端布局。
- [x] 增加稳定标签与保修截止；序列号因隐私边界暂不存储，后续需先升级鉴权模型。
- [x] 提供图片压缩、缩略图、替换和孤儿文件清理。
- [x] 增强键盘导航、焦点样式、表单标签和对比度。
- [x] 将 `98.css` 固定版本并本地化，保证离线可用和构建可复现。

完成标准：主要流程在桌面和手机尺寸均可完成；无鼠标可操作核心功能；页面离线启动时视觉不依赖第三方 CDN。

### M6：统计、货币与导出

- [ ] 设置用户基准币种，并把汇率、来源和更新时间作为配置或数据保存。
- [ ] 明确在售出、仍持有、缺失日期和部分月份情况下的每日成本计算规则。
- [ ] 增加按年份、分类、品牌、状态的可解释统计，数值可追溯到设备列表。
- [ ] 支持 CSV 导出和筛选后导出，并在导出格式中加入 schema 版本。
- [ ] 若接入在线汇率，必须缓存并允许手工覆盖；无网络时继续显示最后一次有效值及时间。

完成标准：统计结果有口径说明；同一输入在前端、导出与测试中得到一致金额；离线时不会假装使用实时汇率。

### M7：生产部署与运维

- [ ] Docker 镜像使用锁文件、非 root 用户、健康检查和明确的持久化卷。
- [ ] 加入安全响应头、请求大小限制、速率限制和反向代理部署说明。
- [ ] 记录结构化日志，但不记录管理密码或敏感设备字段。
- [ ] 提供升级前备份、迁移、健康检查和回滚步骤。
- [ ] 明确本项目仍是单用户本地工具；若要公网多用户化，应另行设计账户、会话、权限和隐私模型，不能在共享密码上继续叠加。

完成标准：容器重建不丢数据；升级流程经过恢复演练；健康检查、日志和备份足以定位常见故障。

## 每次改动的验证清单

最低要求：

```bash
node --check server.js
node --check main.js
node --check modules/api.js
node --check modules/admin.js
node --check modules/render.js
node --check modules/schema.js
node --check modules/store.js
```

按改动范围追加：

- 服务端/API：启动临时实例，检查 `/health`（M0 完成后）、相关成功路径和至少一个失败路径。
- 数据库：复制数据库或使用临时库测试迁移，比较迁移前后记录数，不直接试验仓库快照。
- 前端：实际打开受影响页面，检查浏览器控制台、窄屏布局和含特殊字符的输入。
- 导入导出：先预览，分别验证 append/replace、非法 JSON、缺少名称和中途失败回滚。
- Docker：执行 `docker compose config`，关键部署改动再做一次全新 build 和持久化重启测试。
- 安全：至少用 `<script>`、引号、超长输入、非法 ID、伪装图片和错误密码做负向验证。

当 M3 完成后，所有交付还必须通过 `npm test` 和 CI 对应检查。

## 交付约定

- 一次提交聚焦一个里程碑内的可验证增量，不混入无关重构。
- 交付说明应列出：用户可见变化、数据/环境变化、执行过的验证、未完成风险。
- 若更改数据库或导入格式，必须同时说明迁移、兼容性和回滚方式。
- 完成路线项后把对应复选框更新为 `[x]`；如果实现方案改变，同时维护本文件，避免计划与代码脱节。
