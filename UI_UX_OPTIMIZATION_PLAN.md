# Things Terminal UI / UX 布局优化计划

> 目标：保留 Windows 98 的窗口、标题栏、立体按钮、灰色面板和像素字体语言；不把产品改成现代 SaaS 仪表盘。优化重点是信息层级、尺寸、留白、响应式行为和键盘可用性，让它更像一套整理得当的经典桌面应用。

## 1. 范围与原则

### 保留的 98 风格元素

- 青绿色桌面背景、灰色窗口面板、黑色边框与亮/暗内阴影。
- `title-bar`、`status-bar`、原生 98 风格按钮、下拉框和进度条。
- 本地 Fusion Pixel 字体、直角、低圆角（默认 `0`）和高对比焦点框。
- “设备档案 / 管理控制台”的桌面软件叙事，不改为卡片化营销页。

### 优化原则

1. **先减法，后装饰**：移除无信息价值的大间距和重复容器，不增加渐变、玻璃效果、圆角或动画。
2. **一个页面一个主窗口**：导航为固定的应用框架；内容区只保留一个清晰的主任务窗口，避免窗口套窗口造成边框噪声。
3. **尺寸可预测**：采用统一间距与控件高度 token，避免同类元素在不同页面大小不一致。
4. **移动端是紧凑桌面窗口，不是缩小的桌面页面**：保留视觉语言，但将横向工具栏、双栏表单和批量操作改为可阅读的分组堆叠。
5. **可访问性不妥协**：所有尺寸调整都保留键盘焦点、44px 左右的触控目标、清晰标签和状态反馈。

### 不在本计划中处理

- 不更换前端框架或重写页面路由。
- 不改变 API、数据库字段、导入导出格式或管理权限。
- 不引入第三方 UI 组件库；继续使用已本地化的 `98.css`。

## 2. 当前问题清单

| 区域 | 当前表现 | 影响 | 优化方向 |
| --- | --- | --- | --- |
| 全局页面框架 | `body`、`.page`、`.hero`、`.section` 多层大间距叠加，首页尤为松散 | 首屏信息密度低，用户需要滚动才看到设备 | 收敛纵向节奏，移除空 hero 占位，主内容靠近导航 |
| 重复边框 | 卡片、筛选、表单、预览、详情多处同时使用 2px 外框和阴影 | 视觉层级平均，重点不突出 | 仅窗口/主面板用完整立体边框；内部区块用 `fieldset`、分隔线或凹槽 |
| 导航 | 五个导航项可换行，但缺少当前页面标题和窄屏优先级 | 手机上易形成两行按钮，语义不清 | 保留菜单外观，改为可横向滚动的单行菜单，显示当前模块标题 |
| 首页 | 空 hero 区和 `56px` 区块间隔占用大量高度；排序与过滤控件等权排列 | 首次使用时设备列表不够突出 | 把摘要、排序和筛选合并为紧凑工具栏，设备列表成为主视觉 |
| 搜索/筛选 | 搜索、计数、清空按钮与筛选面板拆为两块且宽度不均 | 操作路径来回跳，筛选结果上下距离较远 | 用单一“查询工具栏 + 可折叠筛选组”，结果数靠近搜索框 |
| 卡片 | 图片占 4:3 且卡片最小高度 220px；标题不换行 | 小屏每张卡片过长，长名称被过度截断 | 固定合理缩略图高度，允许标题最多两行，压缩元信息行距 |
| 新增页 | 所有字段同级呈现，预览与 JSON 代码长期占据一半宽度 | 主任务不明确，长表单疲劳 | 分为“基本信息 / 交易与状态 / 图片与归属 / 备注与规格”；预览折叠或侧栏吸附 |
| 详情页 | 详情顶部另有大 hero，再进入详情面板 | 重复标题，关键信息被下推 | 将返回、名称、状态合为详情窗口标题区域；图片与摘要首屏可见 |
| 统计页 | 指标卡、图表卡视觉重量相同 | 核心指标和可展开解释难以区分 | 采用紧凑 KPI 状态栏 + 图表工作区，明确“可筛选/可追溯”入口 |
| 管理页 | 左侧列表与右侧超长表单并列，分类/批量/导入/汇率混在表单尾部 | 编辑任务与高风险操作混杂，窄屏冗长 | 管理页拆为“资产编辑”和“维护工具”两个窗口；危险操作独立分组并二次确认 |
| 小屏 | 仅在 `720px` 改为单列，控件、内边距、按钮长度基本不变 | 360–430px 宽度下视觉拥挤，横向内容风险高 | 添加 900px、720px、480px 三档响应式规则；优先保证搜索、编辑和保存流程 |

## 3. 统一视觉与尺寸基线（先建立，再逐页替换）

在 `styles.css` 顶部新增以下语义 token；所有后续尺寸优先引用 token，而不是继续写分散的像素值。

```css
:root {
  --page-max: 1120px;
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 24px;
  --space-6: 32px;
  --control-height: 32px;
  --touch-target: 40px;
  --panel-padding: 16px;
  --window-gap: 20px;
  --content-gap: 16px;
}
```

### 尺寸决策

| 元素 | 当前主要尺寸 | 目标尺寸 | 说明 |
| --- | --- | --- | --- |
| 页面最大宽度 | 1200px | 1120px | 降低超宽屏行长，保留桌面工作区感 |
| 页面顶部/底部 | 32px / 64px | 20px / 32px | 提升首屏密度 |
| 页面区块间距 | 36px、56px 混用 | 20px 或 32px | 用 `--window-gap` / `--space-6` 统一 |
| 主面板内边距 | 20–24px | 16px（桌面），12px（小屏） | 保留窗口呼吸感，不浪费垂直高度 |
| 表单输入内边距 | 12px 16px | 6px 8px，最小高度 32px | 更贴近经典桌面控件比例 |
| 主要按钮 | 由 98.css 决定且不一 | 最小 32px，高频触控场景 40px | 不重置 98.css 外观，只补布局约束 |
| 卡片网格 | `minmax(230px, 1fr)` | 240px / 2 列（平板）/ 1 列（手机） | 避免极窄卡片和过宽卡片 |
| 卡片图片 | 4:3 自适应 | 桌面 148px、手机 132px，`object-fit: cover` | 设备信息优先于装饰性大图 |
| 标题字号 | 首页最大 56px | 24–32px | 档案工具不需要海报级标题 |

## 4. 分阶段实施计划

### U0：布局基线与现状截图

**目标**：在改动前形成可比较的视觉基线，并使 CSS 尺寸可统一维护。

- [x] 使用 Playwright 在 1440px、1024px、768px、390px 宽度分别检查六个页面；新增无横向溢出回归测试。
- [x] 记录首屏可见内容、横向溢出、按钮换行、表单滚动长度和焦点顺序。
- [x] 新增上述 CSS token；将 `.page`、`main`、`.section`、`.window-body` 的间距替换为 token。
- [x] 删除首页空的 `.hero__content` 占位及只产生空白的 hero 规则。
- [x] 确认不改变 98.css 的本地加载、颜色变量和立体边框规则。

**验收标准**：六页均无横向滚动条；同类输入、按钮、面板在不同页面高度一致；未改动任何功能行为。

### U1：全局应用框架与导航

**目标**：让用户在任何页面都清楚当前位置、主要任务和可切换模块。

- [ ] 抽取各 HTML 重复的 header 为一个可复用渲染入口，避免五处导航逐渐漂移。
- [x] 将导航菜单设为不换行、可横向滚动的 98 风格菜单；小屏保留完整入口，不隐藏“管理”。
- [x] 为菜单项增加明确的 `aria-current="page"`；保留 `.is-active` 的凹陷状态。
- [x] 把窗口右上角的最小化/最大化/关闭按钮明确为装饰性：加 `type="button"`、禁止获得焦点，不能制造“点击无效”的误导。
- [x] 为首页主窗口改为紧凑的状态栏与模块说明，移除重复静态日期文案。
- [ ] 统一标题文案：英文机器标签可保留在标题栏，中文任务标题作为页面主标题，避免双重大标题。

**验收标准**：390px 下导航可完整访问；Tab 焦点不进入纯装饰按钮；每个页面首屏可辨识当前任务。

### U2：首页、搜索与设备卡片

**目标**：让“看设备”和“找到设备”成为最快的两条路径。

#### 首页

- [x] 标题栏下保留一行总数/使用中/已失去摘要，作为 98 风格状态栏。
- [x] 将排序、子物品显示、类别、品牌压缩为一个 `toolbar`，标签改为紧凑横排；在 720px 以下以两列栅格堆叠。
- [ ] 只显示最常用的两个控件；其余筛选放进“更多筛选”可展开区域，并复用搜索页状态。
- [ ] 设备卡片紧随工具栏，不再使用大型 hero 和 56px 的区块断层。

#### 搜索页

- [x] 搜索输入、结果数、清空按钮归入同一工具栏；输入框在桌面占满剩余宽度。
- [ ] 分类、品牌、状态、标签等筛选按组展示；移动端默认收起非核心组，已生效筛选以可清除标签显示。
- [x] 清空按钮仅在有筛选条件时可用；结果数通过 `aria-live="polite"` 更新。
- [ ] 支持 URL 查询参数与现有 localStorage 偏好同步，刷新和从详情返回时不丢上下文。

#### 卡片

- [x] 采用“缩略图 / 名称与状态 / 两行关键元信息 / 查看详情”固定层级。
- [x] 标题允许最多两行，品牌和分类只显示一行；不再让长设备名单行截断到无法识别。
- [x] 减少卡片内部 `gap`、图片高度和正文 padding；使用统一的 `--space-*` token。
- [ ] 保持状态、保修临期等信息的文字+图标/纹理区分，不能只依赖颜色。
- [ ] 整张卡片或明确的“查看详情”按钮必须有可见焦点与不含歧义的 accessible name。

**验收标准**：1440px 首页首屏至少展示 4 张设备卡；390px 搜索、筛选、清空和第一条结果无需横向滚动；长名称仍可识别。

### U3：新增与详情流程

**目标**：降低录入时的认知负担，并让详情页在第一屏回答“这是什么、目前状态、何时/多少钱获得”。

#### 新增页

- [x] 使用 `<fieldset>` 和 `<legend>` 组织四组字段：基本信息、购入与状态、图片与归属、备注与规格。
- [ ] “名称、分类、状态、入手时间”列为必填/常用组，置于最上方；保修、标签、备注、规格移至可展开的“更多信息”。
- [ ] 将图片 URL 与上传控件合并为“图片来源”组，说明二选一规则和上传成功后的缩略图状态。
- [ ] 预览改为桌面右侧粘性面板；`复制 JSON` 和 JSON 文本默认折叠，移动端放到表单末尾。
- [ ] 保存操作固定在表单末尾；长表单桌面可增加不遮挡内容的“保存草稿/保存”操作条（仅在已有草稿逻辑时实施）。

#### 详情页

- [x] 移除独立的大型 `hero`，在同一个详情窗口里提供“← 返回结果”“设备名称”“状态”。
- [x] 桌面使用 280px 图片列 + 内容列；图片列在小屏位于标题信息之后，且高度受控。
- [x] 将核心信息（品牌、分类、状态、购入时间、价格、持有天数）组成 2 列定义列表；备注、规格、关系和历史为后续分组。
- [ ] 关系设备以紧凑列表显示，链接仍返回保留筛选条件的上下文。
- [ ] 为无图片、无价格、无日期等空字段提供简短占位，不保留大量空白。

**验收标准**：新增页在 390px 可完成录入且保存按钮容易找到；详情页 768px 首屏能看到名称、状态、图片和至少四项关键属性。

### U4：统计与管理后台

**目标**：将分析、编辑和高风险维护操作从视觉与交互上分开。

#### 统计

- [ ] 顶部改为 3–4 个紧凑 KPI（总数、活跃、总投入、每日成本）；口径说明用可聚焦提示窗，不能仅依赖 hover。
- [ ] 图表工作区按“时间 / 分类 / 品牌 / 状态”分区；每个图表旁提供跳转到已筛选设备列表的链接。
- [ ] 控制 `bar-row` 标签列的最大宽度，长品牌使用省略并附完整 title/辅助文本。
- [ ] 基准币种与汇率时间显示在状态栏，离线/过期数据有明确文本提醒。

#### 管理后台

- [ ] 将当前页面重组为两个同级窗口：`资产编辑`（列表 + 编辑表单）和 `维护工具`（分类、批量操作、导入导出、图片清理、汇率）。
- [ ] 在桌面宽度下保持左侧资产列表（约 38%）+ 右侧编辑表单（约 62%）；编辑表单顶部固定当前对象名称与保存状态。
- [ ] 将分类管理、批量更新、导入/导出以可展开 `fieldset` 分组；默认只展开当前操作。
- [ ] “批量删除”“覆盖导入”“清理孤立图片”使用危险操作区域：红色标题栏/警示图标、影响数量、二次确认，不与普通保存按钮并列。
- [ ] 移动端先显示设备选择和编辑表单；维护工具独立在下方，列表高度降为约 38vh。
- [ ] 改善 `admin-row`：名称可两行，次要分类/品牌可见，操作按钮不压缩主文本；多选框有独立可点击标签。

**验收标准**：管理页中普通编辑、批量操作和覆盖导入在视觉上明显分开；1024px 不出现双栏挤压；手机上不会误触危险操作。

### U5：响应式、键盘与视觉回归

**目标**：把布局优化转化为可持续的质量门。

- [x] 设定并测试四个断点：`>= 1200px`、`900–1199px`、`600–899px`、`< 600px`。不要只使用单个 720px 断点。
- [ ] 所有输入、按钮、chip 在触屏断点最小高度 40px；桌面可维持 32px 紧凑高度。
- [ ] 在 `<= 600px`：页面 padding 8–12px、窗口内边距 10–12px、卡片单列、表单单列、价格输入仍保持输入框优先。
- [ ] 在 `600–899px`：卡片至少两列（空间足够时），新增页预览移至表单下方，后台改为上下两段。
- [x] 为跳到主内容增加可见的 skip link；修正菜单 `role` 使用与按钮实际行为；检查所有 label、状态提示与对话确认。
- [ ] 用键盘验证：首页筛选、搜索清空、创建、详情返回、管理选择/保存/危险操作确认。
- [x] 在 Playwright 增加每页四个视口的稳定 DOM 尺寸断言，并人工确认 98 风格未被稀释。

**验收标准**：390px、768px、1024px、1440px 六页没有横向溢出；核心流程不依赖鼠标；视觉回归测试在本地与 CI 稳定通过。

## 5. 建议实施顺序与提交边界

| 提交 | 包含内容 | 不应混入 |
| --- | --- | --- |
| `uiux: establish layout tokens and compact app shell` | U0 + U1 | 数据模型、接口改动 |
| `uiux: improve browse and device card density` | U2 | 管理页重构 |
| `uiux: streamline add and device detail workflows` | U3 | 导入导出逻辑 |
| `uiux: separate admin maintenance operations` | U4 | 安全策略变更 |
| `test: add responsive visual regression coverage` | U5 | 非视觉功能 |

每个提交后执行：

```bash
npm run check
npm test
npm run test:browser
git diff --check
```

涉及 CSS 和 HTML 的提交还需在 390px、768px、1024px、1440px 实际检查首页、搜索、新增、详情、统计、管理六页。

## 6. 验收清单

- [ ] 98.css、像素字体、本地资源和经典立体控件仍生效，未引入圆角卡片、渐变或玻璃化效果。
- [ ] 首屏不再包含无内容的大面积 hero/留白；首页设备列表更靠前。
- [ ] 所有页面使用一致的间距、控件高度和面板内边距。
- [ ] 长名称、分类和品牌在卡片、详情与后台均可读，不能只剩无意义省略号。
- [ ] 搜索、筛选、结果数和清空操作在同一可理解区域；详情返回不丢筛选上下文。
- [ ] 新增流程按任务分组，预览不抢占移动端主操作空间。
- [ ] 管理后台的危险操作明显隔离，并保持现有鉴权与确认逻辑。
- [ ] 390px 至 1440px 无横向滚动；触控目标、焦点可见性和键盘顺序通过验证。
- [ ] 所有现有 API、导入导出、图片上传、测试与 Docker 预览行为保持兼容。

## 7. 后续记录方式

实现开始后，在 `AGENTS.md` 的 M5 条目下追加链接到本计划，并按 U0–U5 更新完成状态。若视觉方案和本计划不一致，应先更新本文件再实现，避免后续 Agent 只根据截图猜测设计意图。
